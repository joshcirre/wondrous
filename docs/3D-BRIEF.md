Implement ONLY resources/js/components/Battlefield.tsx and optional miniatures.tsx. Work immediately; stop researching. Parent awaiting component for browser verification. React19 fiber9 drei10 three installed.
Export default Battlefield with props:
{units:Array<{id:string;character_id:string;owner_id:number;x:number;y:number;hp:number;max_hp:number;mana:number;facing?:string;recovery?:number}>,viewerId:number,selectedId:string|null,onSelect:(id:string)=>void,onTile:(x:number,y:number)=>void,highlights?:Array<{x:number;y:number;kind?:string}>,deployment?:boolean,interactive?:boolean}.
Board8x8. world x-3.5,z=y-3.5, host player y6/7 home. Camera toward board from positive z; auto flip if viewer not host? cannot know host except units facing default; optional leave fixed. Orthographic Canvas camera responsive zoom using viewport size, OrbitControls bounded angle/zoom, no pan. Container outer width,height100% minHeight0. All 64 tiles clickable onTile. Miniatures onSelect stopPropagation. Highlights move blue-green attack red skill purple. Selected ring. Health tiny bars. Smooth movement and idle breathing via useFrame.
Produce colorful medieval tabletop miniature meshes with defined heads helmets robes armor shields swords capes etc for 12 ids: warden,ranger,knight,arcanist,cleric,rogue,pikeman,herald,pyromancer,frostweaver,druid,revenant. Real 3D meshes (no flat pictures), distinct archetype silhouettes and team base trims. Raised carved stone/wood board plinth with moss sandstone tiles, corner braziers fire particles, surrounding ruins / trees. Gentle shadows, directional sunlight, atmospheric background. Asset details moderate. No external font/model/HDR URLs. No Text that fetches external fonts. Don't read whole CSS (one huge line); known parents .lobby-board height516 .battle-canvas minHeight570 absolute child fill.
Write files NOW then typecheck before polish, no planning delay. Don't change package json or other app source, no agents. Notes docs/ARTIST-NOTES.md.
